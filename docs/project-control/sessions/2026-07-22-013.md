# Session 2026-07-22-013

## Objective

Close the OCI release/recovery gates and reconcile the project control records with executed evidence.

## Completed

- Merged PR #17, fixing recovery database backup to resolve the API container through Compose and correcting previous-release metadata capture.
- CI run `29936093386` passed all jobs: API build/tests, frontend checks/build, Browser E2E, Compose validation, and dependency audit.
- Recovery run `29936316021` passed the validation gate and rollback job.
- Rollback created a protected current database backup, rebuilt both HealthCore services, validated Nginx and service health, and restored production from `b3c6437...` to `d914f1d...`.
- SQLite backup/restore and integrity checks passed in previous recovery run `29935334880`.
- Batuara public endpoint, Nginx syntax, and Batuara API/public-site/database health checks passed before and after the operations.
- Removed generated history export/index/rewrite artifacts from the local HealthCore root. The private OCI key remains ignored and ACL-protected.

## Current State

- Production HealthCore release: `d914f1d9e523c0f53fc2069b5579fbf37fe63966`.
- Repository main: `b3c64373a81e39d31a1fb42c77f2fe37e5d8c035`.
- Public route: `/healthcore/`.
- Estimated completion: 90%.

## Pending

- Install a newly generated OCI SSH public key, update `OCI_SSH_PRIVATE_KEY`, and revoke the historical authorized key.
- Rotate application/admin credentials and invalidate sessions where historical exposure is confirmed.
- Decide whether an independent OCI staging stack is required beyond containerized CI staging.
- Run the final audit and closure report after the credential gates are evidenced.

## Safety Note

No Batuara file, configuration, container, or deployment was modified by HealthCore workflows.
