# Session 2026-07-21-003

## Scope

Continue the approved HealthCore hardening loop inside `C:\Users\MarcoGuelfi\Projetos\HealthCore` only. No other local project was read or modified. No OCI write, deployment, Nginx reload, SSH session, or Batuara operation was performed.

## Implementation

- Removed the duplicate `/api/usuarios` and `/api/especialidades` endpoint blocks after verifying current frontend usage of `/users` and `/especialidades`.
- Corrected stale references to database population scripts after their move under `scripts/database/`.
- Removed the local SSH private-key copy and replaced the legacy documentation key sample with a GitHub Secret placeholder.
- Generated test idempotency keys at runtime to remove Gitleaks false positives from data-seeding fixtures.
- Hardened the frontend container: Nginx runs as `nginx`, listens on 8080, uses a writable `/tmp/nginx.pid`, resolves the Docker API upstream lazily, and returns tighter security headers/CSP.

## Validation

- `.NET SDK 8.0.422` container: build and tests pass, `84/84`.
- Frontend Node 22 container: install, type-check, lint, tests, and build pass; lint has 98 warnings and Sass has four deprecation groups.
- Final API and frontend Docker image builds pass.
- Compose config passes with ephemeral JWT configuration.
- API smoke passes: live 200, health 200, protected patients 401 without token.
- Integrated smoke passes: frontend health 200, app page 200, API proxy without token 401, frontend user `nginx`.
- Trivy final API and frontend images: zero HIGH/CRITICAL findings detected with unfixed findings ignored.
- Gitleaks current working tree: no leaks found.

## Findings and Gates

- Historical Gitleaks scan identified eight findings in old commits/paths. Values were not exposed in the report. Credential rotation and history rewrite remain Gate B activities.
- GitHub Actions has been defined locally but not executed in the authenticated repository context.
- OCI/Nginx configuration inspection remains pending until a secure SSH credential is supplied through an approved channel. No shared VPS state was changed.
- Browser E2E, production persistence validation, OCI staging, promotion, and rollback remain open.

## Session Status

Local implementation and validation are substantially complete, but the project is not production-ready. Current control estimate at session close: 12/28 work items done locally, 6 in progress, 10 pending; approximately 54% weighted completion. The closure Trivy re-scan is blocked by Docker containerd I/O and remains open.