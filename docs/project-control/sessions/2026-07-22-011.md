# Session 2026-07-22-011

## Scope

Continue the HealthCore implementation loop in the project workspace. OCI access was limited to read-only inspection explicitly requested by the user. Batuara.net was not modified.

## Implementation

- Added an explicit `.gitignore` entry for `ssh-key-2025-08-28.pem`.
- Restricted the Windows ACL of the local OCI private key to the current user; the key contents were never read or printed.
- Standardized the public frontend route from `/healthcore-frontend/` to `/healthcore/` in Vite, Docker build arguments, Compose, Nginx, favicon, and E2E coverage.
- Added the OCI CD workflow and release script controls for main-branch authorization, dirty-tree refusal, Batuara preflight, database backup, targeted Nginx validation, HealthCore smoke checks, and temporary-file cleanup.

## OCI read-only evidence

- SSH succeeded.
- Batuara containers and public domains were healthy before any HealthCore change.
- Containerized Nginx configuration validation passed.
- HealthCore API readiness passed with healthy database, filesystem, and database-performance checks.
- The remote HealthCore Git tree is dirty and remains an explicit deployment blocker.
- No OCI deployment, Nginx reload, database mutation, or Batuara operation was performed.

## Validation

- Frontend production bundle: pass in the Node container with `VITE_BASE_PATH=/healthcore/`.
- Frontend type-check: pass.
- Frontend lint: pass with zero errors and warnings.
- Frontend tests: 10/10 after rerun.
- Ephemeral Nginx route validation: `/healthcore` redirects to `/healthcore/`, `/healthcore/` and `/health` return 200, and protected `/api/v1/pacientes` returns 401.
- Playwright route smoke: 4/4 against the ephemeral Nginx container.
- Docker image build was not conclusive because Docker Desktop BuildKit repeatedly terminated with `rpc error ... EOF` during the Vite build; the source build and proxy validation passed independently.

## Status

Local implementation remains 22/28 work items done (78.6%). The route standardization is locally validated. Production completion remains blocked by external approval and release gates, not by the route implementation.