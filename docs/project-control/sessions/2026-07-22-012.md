# Session 2026-07-22-012

## Contributor audit

- The local and `origin/main` histories contain 166 reachable commits authored by `guelfi`, `Marco Guelfi`, or `Guelfi <guelfi@MacBook-Pro-de-Guelfi.local>`.
- The 11 commits using `Guelfi@MacBook-Pro-de-Guelfi.local` are attributable to the project owner and can be normalized to `Marco Guelfi <guelfi@msn.com>` if a history rewrite is approved.
- The current history contains one `Co-Authored-By: Warp <agent@warp.dev>` trailer.
- GitHub's public commit queries returned zero commits for `mmcarvaxd`, `qwen-coder`, and `warp-agent` in the current repository history.
- The public contributors API currently returns only `guelfi` and one anonymous author, matching the owner-local identity. The four-avatar screenshot is therefore stale, cached, or based on historical GitHub metadata.

## Decision

GitHub does not provide a button to delete a contributor. Removing valid attribution would be inappropriate. The only technically relevant cleanup is an optional Gate B history rewrite that normalizes the owner's old local identity and removes the obsolete AI co-author trailer, followed by a reviewed force-push. This has not been executed.

## External blockers

- The local GitHub CLI credential is invalid, so no authenticated push, workflow dispatch, or repository settings change was performed.
- The remote OCI HealthCore tree remains dirty and the prepared CD workflow correctly refuses deployment until it is reconciled.
- The plan cannot be marked 100% until credential rotation, database backup/restore validation, OCI staging/promotion/rollback, and final closure evidence are completed.